[pytest]
# 项目运行时 PYTHONPATH=/app（参见 app/Dockerfile + main.py 中 `from core.config ...`）
# pytest 对齐这一约定，让 `from core...` / `from services...` 在测试中可解析。
pythonpath = app
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
